{
	"version": 2,
	"buildCommand": "npm run server build -- -- --outDir ../../build && npm run vue build -- -- --outDir ../../build/views",
	"outputDirectory": "build",
	"builds": [
		{
			"src": "build/index.js",
			"use": "@vercel/node",
			"config": { "includeFiles": ["build/**"] }
		}
	],
	"routes": [
		{
			"src": "/(.*)",
			"dest": "build/index.js"
		}
	]
}
